{
  "name": "yakinpintar-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd backend && npm install && npm start",
    "build": "cd frontend && npm install && npm run build"
  },
  "devDependencies": {
    "wrangler": "^4.65.0"
  }
}
