name = "yakinpintar-backend"
main = "src/server.js"
compatibility_date = "2024-02-14"

[vars]
# Add non-sensitive vars here
# Sensitive vars (SUPABASE_KEY, JWT_SECRET, etc.) should be added via Cloudflare Dashboard or 'wrangler secret put'

[env.production]
# Production specific variables
NODE_ENV = "production"
